import { BuscarConvenioPipe } from './buscar-convenio.pipe';

describe('BuscarConvenioPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarConvenioPipe();
    expect(pipe).toBeTruthy();
  });
});
