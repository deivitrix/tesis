import { BuscarHistorialPipe } from './buscarHistorial.pipe';

describe('BuscarHistorialPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarHistorialPipe();
    expect(pipe).toBeTruthy();
  });
});
