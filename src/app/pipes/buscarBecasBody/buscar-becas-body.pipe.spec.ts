import { BuscarBecasBodyPipe } from './buscar-becas-body.pipe';

describe('BuscarBecasBodyPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarBecasBodyPipe();
    expect(pipe).toBeTruthy();
  });
});
