import { BuscarBecasPipe } from './buscar-becas.pipe';

describe('BuscarBecasPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarBecasPipe();
    expect(pipe).toBeTruthy();
  });
});
